---
title: Standup — 2026-06-10
type: standup
date: 2026-06-10
publish: false
attendees: [jack, mei, dani, omar, priya, sam]
---

# Standup — 2026-06-10

## Since last time

- **MCP server**: auth path is working end-to-end against `localhost:13345`.
  OAuth 2.1 (Authorization Code + PKCE) lands tokens; scoped to notes + kanban.
- **Web board view**: drag-and-drop between columns is stable; WIP limits next.
- **CLI**: `jtype sync` write-through verified against a bound workspace.

## Today

- Mei: lock the `create_card` / `move_card` tool contracts.
- Dani: finish the search index so `search_notes` returns titles + snippets.
- Omar: draft launch positioning (waiting on a card — see below).
- Priya: docs skeleton for the AI integration.
- Sam: custom-domain TLS spike (Q4, low priority for now).

## Blockers

- None hard-blocking. Launch planning has no owner yet — needs a card on the
  **Launch** board so it doesn't fall through the cracks.

## Decisions

- We'll run the kickoff for the AI integration on **2026-06-14** and pull the
  Q3 items straight from [roadmap.md](../roadmap.md).
- One workspace for notes + board. No more scattered docs.

## Action items

- [ ] Create a "Draft launch plan" card on the Launch board (high priority).
- [ ] Schedule the AI integration kickoff for Fri 06-14.
- [x] Confirm the MCP token is mcp-scoped only (no admin). — verified by Mei.
