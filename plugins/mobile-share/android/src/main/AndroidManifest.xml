<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <provider
            android:name="net.jcode.jtype.mobileshare.MobileShareFileProvider"
            android:authorities="${applicationId}.mobile-share.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/jtype_mobile_share_paths" />
        </provider>
    </application>
</manifest>
