[default]
description = "Allows the shared JType app to register native push tokens and consume canonical notification routes."
permissions = [
  "allow-registration",
  "allow-takePendingRoute",
  "allow-registerListener",
  "allow-removeListener",
]
