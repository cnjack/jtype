pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = "tauri-plugin-mobile-push"
include(":tauri-android")
project(":tauri-android").projectDir = File("../../../src-tauri/gen/android/tauri-android")
