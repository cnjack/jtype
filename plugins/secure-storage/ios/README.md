# JType Secure Storage iOS Adapter

Stores JType mobile credentials as generic passwords in the system Keychain.
The Swift API is registered as an internal Tauri plugin and is not exposed to
the webview.
